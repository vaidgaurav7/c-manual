#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

       int i,j,k,l,n;
       scanf("%d\n",&n);
       
       for(i=0;i<n;i++)
              {
             char a[200];
              gets(a);
              
              if((a[0]=='h'||a[0]=='H')&&(a[1]=='I'||a[1]=='i')&&(a[2]==' ')&&(a[3]!='d'&&a[3]!='D'))
                     {
                     printf("%s\n",a);
                     }
              
             }
       
       
      
    return 0;
}
